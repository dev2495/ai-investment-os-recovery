# Skills and routines — preserved baseline and pending M7

Reuse existing skill definitions, `agent.workflow_schedules`, schedule runs/materializer, daemon supervision and the company research monitor. No new scheduler or recurring task has been installed by this increment.

The lease reaper currently executes within an opted-in general-worker pass. That is not acceptance of a separately supervised maintenance routine or a 24-hour operating cadence.

M7 must persist version, owner, scope, schedule, last/next run, idempotency key, bounded budget, allowed tools, timeout, cooldown, retry policy, receipt and operator control. A routine tick may create bounded work only inside its recorded permissions. Duplicate filings/catalysts must not create duplicate research or notifications.

Required routine set, all pending live acceptance:

1. Office/worker health sweep.
2. Managed Obsidian synchronization preserving human text.
3. Existing-following research/filing update review with deduplication and decision history.
4. Receipt-aware stale-lease reconciliation.
5. Existing Zerodha health/freshness check, with daily authentication remaining human-owned.

No routine may purchase model use, promote routes, resolve investment decisions, write broker orders or repair credentials by itself. These are product-stack routines, not Codex-thread reminder automations.
