# Phase 2 reuse matrix

Source baseline: `b42cc5d`. No production schema inventory was possible on 2026-09-04.

| Existing authority | This increment | Later work |
|---|---|---|
| `agent.profiles`, hierarchy/departments | Extend keys/version/concurrency; preserve IDs and display configuration | Full role/tool/privacy snapshots, cross-desk capability mapping |
| `agent.tasks`, worker queue and `worker_runs` | Add linked ownership/steps/events; wrap existing general worker | All task-class/receipt adapters and live cutover |
| Research Case execution/source workers | Unchanged, including approval/budget/review gates | Separate receipt-aware integration, never blanket retries |
| Graph definitions/runs/checkpoints | Existing completion/output semantics preserved | Charlie persisted planning and redirects |
| Messages/mailboxes and committees | Unchanged; no parallel queue | Thread acknowledgements and durable handoffs |
| Model registry/routes/assignments/usage | Unchanged; provider gates preserved | Qualified versioned bindings and explicit failover |
| Workflow schedules and research monitor | Unchanged; no second scheduler | Versioned idempotent routine contracts and demos |
| API and MCP servers | Add domain modules, compatibility endpoints and two narrow MCP tools | Full requested commands through the same authorization |
| Office snapshot/query/2D/3D shell | Extend projection with lease truth and bounded events/control | Complete detail parity, handoffs, performance and live acceptance |
| Existing LaunchAgents | Deployment copy helper includes new API modules; no service started | Canary supervision, drain/restart and soak |
| Obsidian/indexes | No local private-vault copy | Managed notes on canonical SSD after reconnection |
| Zerodha/market/portfolio surfaces | No changes | Read-only health verification only |

New tables are ownership, audit and dependency records referencing canonical task/profile IDs. They are not a second task, message, model or office control plane.

