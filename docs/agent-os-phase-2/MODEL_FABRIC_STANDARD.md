# Model Fabric — preserved baseline and pending M5

No model route, default, provider, credential source, price assumption or paid-call approval has been changed in this increment. New runtime controls do not invoke a model.

Existing model routes/catalog/assignments, aliases, task-routing registry, usage/cap ledgers and Research preflight/canary gates remain authoritative. The general worker still executes its existing provider checks before analysis. A lease is ownership, not model permission.

M5 must extend those authorities with versioned per-agent/task-class bindings and immutable execution receipts containing requested alias, resolved model/provider, qualification version, privacy class, token/cost bounds, latency, fallback decision and review outcome. User-mentioned model names are not proof of current provider availability or equivalent capability.

Required acceptance remains pending: verify actual local MLX/Ollama or approved provider identities; qualify a bounded route; change one Research agent's binding; record the resolved route; stop it and prove explicit fail-closed or separately approved fallback. No silent paid fallback, no client-private cloud egress, no automatic promotion or credential request in chat.

The required external-project comparison and revision/license/security audit remain pending. No external agent project was installed, vendored or granted execution authority here.
