{
  "candidate_id": null,
  "failure_reason": "ERROR:  duplicate key value violates unique constraint \"strategy_candidates_name_key\"\nDETAIL:  Key (name)=(Research-sourced strategy: TATASTEEL watchlist) already exists.\nCONTEXT:  SQL statement \"INSERT INTO strategy.strategy_candidates (\n        name,\n        source_kind,\n        source_ref,\n        hypothesis,\n        universe,\n        timeframe,\n        entry_rules,\n        exit_rules,\n        risk_rules,\n        status,\n        owner_agent,\n        intake_id,\n        generated_idea_id,\n        candidate_key,\n        structured_spec,\n        validation_status,\n        activation_gate\n    )\n    VALUES (\n        v_strategy_name,\n        'strategy_intake',\n        v_intake_key,\n        trim(p_intake_text),\n        nullif(trim(coalesce(p_universe, '')), ''),\n        coalesce(nullif(trim(p_timeframe), ''), 'mixed'),\n        jsonb_build_object('draft_rules', trim(p_intake_text), 'needs_parser_review', true),\n        jsonb_build_object('needs_stop_or_exit', true, 'needs_time_exit', true),\n        jsonb_build_object('paper_first', true, 'max_loss_required', true, 'human_approval_required', true),\n        'research',\n        'Backtest Engineer',\n        v_intake_id,\n        v_idea_id,\n        v_candidate_key,\n        jsonb_build_object(\n            'intake_id', v_intake_id,\n            'idea_id', v_idea_id,\n            'strategy_name', v_strategy_name,\n            'symbols', coalesce(p_symbols, ARRAY[]::TEXT[]),\n            'requested_outputs', coalesce(p_requested_outputs, ARRAY['structured_spec','candidate','backtest_queue','validation_review']::TEXT[]),\n            'activation_sequence', ARRAY['structure_rules','baseline_backtest','optimizer_if_promising','validation_review','paper_alerts','human_approval']::TEXT[]\n        ),\n        'not_started',\n        'paper_first_backtest_required'\n    )\n    RETURNING id\"\nPL/pgSQL function strategy.create_strategy_arsenal_intake(text,text,text,text,text,text[],text,text,text[],text,text,text[],text,text) line 124 at SQL statement",
  "generated_at": "2026-07-06T22:14:06.439523+00:00",
  "live_execution_allowed": false,
  "run_key": "discovery_api_smoke_20260707_opt_1_research_sourced_strategy_tatasteel_watchlist",
  "seed_data_allowed": false,
  "stage_results": {
    "failure": {
      "message": "ERROR:  duplicate key value violates unique constraint \"strategy_candidates_name_key\"\nDETAIL:  Key (name)=(Research-sourced strategy: TATASTEEL watchlist) already exists.\nCONTEXT:  SQL statement \"INSERT INTO strategy.strategy_candidates (\n        name,\n        source_kind,\n        source_ref,\n        hypothesis,\n        universe,\n        timeframe,\n        entry_rules,\n        exit_rules,\n        risk_rules,\n        status,\n        owner_agent,\n        intake_id,\n        generated_idea_id,\n        candidate_key,\n        structured_spec,\n        validation_status,\n        activation_gate\n    )\n    VALUES (\n        v_strategy_name,\n        'strategy_intake',\n        v_intake_key,\n        trim(p_intake_text),\n        nullif(trim(coalesce(p_universe, '')), ''),\n        coalesce(nullif(trim(p_timeframe), ''), 'mixed'),\n        jsonb_build_object('draft_rules', trim(p_intake_text), 'needs_parser_review', true),\n        jsonb_build_object('needs_stop_or_exit', true, 'needs_time_exit', true),\n        jsonb_build_object('paper_first', true, 'max_loss_required', true, 'human_approval_required', true),\n        'research',\n        'Backtest Engineer',\n        v_intake_id,\n        v_idea_id,\n        v_candidate_key,\n        jsonb_build_object(\n            'intake_id', v_intake_id,\n            'idea_id', v_idea_id,\n            'strategy_name', v_strategy_name,\n            'symbols', coalesce(p_symbols, ARRAY[]::TEXT[]),\n            'requested_outputs', coalesce(p_requested_outputs, ARRAY['structured_spec','candidate','backtest_queue','validation_review']::TEXT[]),\n            'activation_sequence', ARRAY['structure_rules','baseline_backtest','optimizer_if_promising','validation_review','paper_alerts','human_approval']::TEXT[]\n        ),\n        'not_started',\n        'paper_first_backtest_required'\n    )\n    RETURNING id\"\nPL/pgSQL function strategy.create_strategy_arsenal_intake(text,text,text,text,text,text[],text,text,text[],text,text,text[],text,text) line 124 at SQL statement",
      "stage": "dict_keys([])"
    }
  },
  "status": "failed"
}